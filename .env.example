# Local development uses the Vite proxy and talks to http://localhost:5000 by default.
# Set these only when you want the browser to call a deployed API directly.
VITE_API_BASE_URL=https://api.yourdomain.com/api
VITE_USE_REMOTE_API=true
